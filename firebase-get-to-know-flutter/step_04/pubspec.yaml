name: gtk_flutter
description: Get to know Firebase with Flutter
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^1.1.0 # new
  cloud_firestore: ^2.0.0 # new
  firebase_auth: ^1.1.3 # new
  google_fonts: ^2.0.0
  provider: ^5.0.0 # new

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/codelab.png
